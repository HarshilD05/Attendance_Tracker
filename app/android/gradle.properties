org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Enable Kotlin incremental compilation (required for KGP 2.x cache coherence)
kotlin.incremental=true
# Enable parallel project execution
org.gradle.parallel=true
# Managed by Flutter tool — do not remove; required while migrating to AGP 9 Built-in Kotlin
android.builtInKotlin=false
# Managed by Flutter tool — do not remove; required while migrating to AGP 9 new DSL
android.newDsl=false
